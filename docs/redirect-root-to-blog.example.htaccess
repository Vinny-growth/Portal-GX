# USE APENAS SE A RAIZ PRECISAR REDIRECIONAR TEMPORARIAMENTE PARA /blog.
# NAO HABILITE ESTE SNIPPET ENQUANTO A NOVA HOME PRECISAR RESPONDER EM /.

RewriteEngine On

RewriteCond %{REQUEST_URI} ^/?$
RewriteRule ^$ /blog [R=301,L]
