# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp

# Node (caso adicione build tools depois)
node_modules/
dist/
.env
.env.local

# Logs
*.log
npm-debug.log*

# Reviews / scratch
.review/
scraps/

# Secrets / deploy scripts (contain credentials — never commit)
do_push.sh
github_push.sh
github_deploy_key
*.pem
*.key
id_rsa

# CodeIgniter runtime (cache, logs, sessions)
writable/

# User-generated uploads (served from disk, not versioned)
uploads/
